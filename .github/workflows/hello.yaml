name: hello world
on:
  push:
    branches:
      - main
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: chekcout code
        uses: actions/checkout@v3
      - name: print message
        run: echo "hello world"
    
